import book from "@image/svg/book.svg";
import { CenterIcon } from "./Icon";

const BookIcon = () => {
  return <CenterIcon src={book} alt={"book icon"} height={20} width={20} />;
};

export { BookIcon };
