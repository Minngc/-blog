import { CenterIcon } from "./Icon";
import cloudMusic from "@image/svg/cloudMusic.svg";

const CloudMusicIcon = () => {
  return (
    <CenterIcon
      src={cloudMusic}
      alt={"cloud music icon 16"}
      width={20}
      height={20}
    />
  );
};

export { CloudMusicIcon };
