import daily from "@image/svg/daily.svg";
import { CenterIcon } from "./Icon";

const DailyIcon = () => {
  return <CenterIcon src={daily} alt={"daily icon "} width={20} height={20} />;
};

export {DailyIcon}