import { CenterIcon } from "./Icon";
import envelope from "@image/svg/envelope.svg";

const EnvelopeIcon = () => {
  return (
    <CenterIcon src={envelope} alt={"envelop icon 16"} width={20} height={20} />
  );
};

export {EnvelopeIcon}