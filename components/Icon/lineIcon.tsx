import line from "@image/svg/line.svg";
import { CenterIcon } from "./Icon";

const LineIcon = () => {
  return <CenterIcon src={line} alt={"line icon 16"} width={20} height={20} />;
};

export { LineIcon };
